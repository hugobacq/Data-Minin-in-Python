# Data-Minin-in-Python

This repo is for our machine learning module assessment (Basic level). We split the work as follows:

Wine Quality (kNN): Done by Hugo. It uses StandardScaler to handle the chemical features before running the kNN algorithm.

Movie Reviews (Naive Bayes): Done by Maxime. It uses MultinomialNB for sentiment analysis. Since the original IMDB dataset from Kaggle was too big for GitHub (>25MB), we used a 10k rows sample instead.

Peer Review
We followed the instructions and reviewed each other's notebooks:

Maxime checked Hugo's kNN and suggested testing different values for 'k'.

Hugo checked Maxime's NB and suggested trying TfidfVectorizer next time.
